# Development environment configuration
# This file contains environment-specific values for development

# Override with actual dev values as needed
config:
  jwt:
    secret: "dev-jwt-secret-change-me"
  
  composio:
    apiKey: "dev-composio-api-key"
    apiBaseUrl: "https://backend.composio.dev/api"
  
  email:
    username: "dev@action-agent.com"
    password: "dev-email-password"

# Development image tags
apiGateway:
  image:
    tag: "dev"

userService:
  image:
    tag: "dev"

aiService:
  image:
    tag: "dev"

extensionService:
  image:
    tag: "dev"
