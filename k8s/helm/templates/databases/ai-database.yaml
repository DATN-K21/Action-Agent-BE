{{- if and .Values.databases.aiDatabase.enabled (not .Values.external.aiDatabase.enabled) }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "action-agent.fullname" . }}-ai-db
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "action-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: ai-db
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      {{- include "action-agent.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: ai-db
  template:
    metadata:
      labels:
        {{- include "action-agent.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: ai-db
    spec:
      securityContext:
        {{- toYaml .Values.databases.aiDatabase.podSecurityContext | nindent 8 }}
      containers:
        - name: ai-db
          securityContext:
            {{- toYaml .Values.databases.aiDatabase.securityContext | nindent 12 }}
          image: "{{ .Values.databases.aiDatabase.image.repository }}:{{ .Values.databases.aiDatabase.image.tag }}"
          imagePullPolicy: {{ .Values.databases.aiDatabase.image.pullPolicy }}
          ports:
            - name: ai-db
              containerPort: {{ .Values.databases.aiDatabase.service.port }}
              protocol: TCP
          env:
            - name: POSTGRES_DB
              value: {{ .Values.databases.aiDatabase.database | quote }}
            - name: POSTGRES_USER
              value: {{ .Values.databases.aiDatabase.auth.username | quote }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.databases.aiDatabase.auth.password | quote }}
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            {{- toYaml .Values.databases.aiDatabase.resources | nindent 12 }}
          {{- if .Values.databases.aiDatabase.persistence.enabled }}
          volumeMounts:
            - name: ai-db-data
              mountPath: /var/lib/postgresql/data
          {{- end }}
          livenessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - exec pg_isready -U "{{ .Values.databases.aiDatabase.auth.username }}" -d "{{ .Values.databases.aiDatabase.database }}" -h 127.0.0.1 -p {{ .Values.databases.aiDatabase.service.port }}
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6
          readinessProbe:
            exec:
              command:
                - /bin/sh
                - -c
                - -e
                - exec pg_isready -U "{{ .Values.databases.aiDatabase.auth.username }}" -d "{{ .Values.databases.aiDatabase.database }}" -h 127.0.0.1 -p {{ .Values.databases.aiDatabase.service.port }}
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 5
            failureThreshold: 3
      {{- if .Values.databases.aiDatabase.persistence.enabled }}
      volumes:
        - name: ai-db-data
          persistentVolumeClaim:
            claimName: {{ include "action-agent.fullname" . }}-ai-db-pvc
      {{- end }}
      {{- with .Values.databases.aiDatabase.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.databases.aiDatabase.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.databases.aiDatabase.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}

{{- if .Values.databases.aiDatabase.persistence.enabled }}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "action-agent.fullname" . }}-ai-db-pvc
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "action-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: ai-db
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.databases.aiDatabase.persistence.size }}
  {{- if .Values.databases.aiDatabase.persistence.storageClass }}
  storageClassName: {{ .Values.databases.aiDatabase.persistence.storageClass }}
  {{- end }}
{{- end }}

---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "action-agent.fullname" . }}-ai-db
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "action-agent.labels" . | nindent 4 }}
    app.kubernetes.io/component: ai-db
spec:
  type: ClusterIP
  ports:
    - name: ai-db
      port: {{ .Values.databases.aiDatabase.service.port }}
      targetPort: ai-db
      protocol: TCP
  selector:
    {{- include "action-agent.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: ai-db
{{- end }}
