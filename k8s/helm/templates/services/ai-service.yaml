{{- if .Values.aiService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "action-agent.fullname" . }}-ai-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{ include "action-agent.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.aiService.replicaCount }}
  selector:
    matchLabels:
      app: ai-service
  template:
    metadata:
      labels:
        app: ai-service
    spec:
      containers:
        - name: ai-service
          image: {{ include "action-agent.image" (dict "root" . "repository" .Values.aiService.image.repository "tag" .Values.aiService.image.tag) }}
          ports:
            - containerPort: {{ .Values.aiService.service.port }}
          envFrom:
            - secretRef:
                name: {{ include "action-agent.secretName" (dict "root" . "name" .Values.secrets.aiService) }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "action-agent.fullname" . }}-ai-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{ include "action-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.aiService.service.type }}
  selector:
    app: ai-service
  ports:
    - port: {{ .Values.aiService.service.port }}
      targetPort: {{ .Values.aiService.service.port }}
{{- end }}
