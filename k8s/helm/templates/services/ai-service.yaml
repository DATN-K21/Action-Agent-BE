{{- if .Values.aiService.enabled }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "aa.fullname" . }}-ai-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "aa.labels" . | nindent 4 }}
    component: ai-service
spec:
  replicas: {{ .Values.aiService.replicaCount }}
  selector:
    matchLabels:
      {{- include "aa.labels" . | nindent 6 }}
      component: ai-service
  template:
    metadata:
      labels:
        {{- include "aa.labels" . | nindent 8 }}
        component: ai-service
    spec:
      containers:
        - name: ai-service
          image: {{ include "aa.image" (dict "root" . "image" .Values.aiService.image) }}
          ports:
            - containerPort: {{ .Values.aiService.service.port }}
              name: http
          envFrom:
            - secretRef:
                name: {{ .Values.secrets.aiService }}
          resources:
            {{- toYaml .Values.aiService.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "aa.fullname" . }}-ai-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "aa.labels" . | nindent 4 }}
    component: ai-service
spec:
  type: {{ .Values.aiService.service.type }}
  selector:
    {{- include "aa.labels" . | nindent 4 }}
    component: ai-service
  ports:
    - port: {{ .Values.aiService.service.port }}
      targetPort: {{ .Values.aiService.service.port }}
      name: http
{{- end }}
