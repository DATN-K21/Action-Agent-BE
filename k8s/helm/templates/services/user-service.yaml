{{- if .Values.userService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "action-agent.fullname" . }}-user-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{ include "action-agent.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.userService.replicaCount }}
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: {{ include "action-agent.image" (dict "root" . "repository" .Values.userService.image.repository "tag" .Values.userService.image.tag) }}
          ports:
            - containerPort: {{ .Values.userService.service.port }}
          envFrom:
            - secretRef:
                name: {{ include "action-agent.secretName" (dict "root" . "name" .Values.secrets.userService) }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "action-agent.fullname" . }}-user-service
  namespace: {{ .Values.global.namespace }}
  labels:
    {{ include "action-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.userService.service.type }}
  selector:
    app: user-service
  ports:
    - port: {{ .Values.userService.service.port }}
      targetPort: {{ .Values.userService.service.port }}
{{- end }}
