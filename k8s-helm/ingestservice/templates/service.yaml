apiVersion: v1
kind: Service
metadata:
  name: {{ include "ingestservice.fullname" . }}
  labels:
    {{- include "ingestservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetport }}
      protocol: TCP
  selector:
    {{- include "ingestservice.selectorLabels" . | nindent 4 }}
